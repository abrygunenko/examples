package com.softserve.session5.example1;

/* The com.softserve.session5.example1.Figure class must be declared as abstract because it contains an abstract method */
public abstract class Figure {
    /* because this is an abstract method the body will be blank */
    public abstract double getArea();
}
